<h2>商品一覧</h2><span>(全<%= @quantity %>件)</span>
<% @items.each do |item| %>
 <%= link_to item_path(item.id) do %>
  <%= attachment_image_tag item, :image, size: "120x120" %>
 <% end %>
 <%= item.name %>
  ¥<%= item.add_tax_price.to_s(:delimited) %>
<% end %>
<%= paginate @items %>

