<div class="container my-4" >
  <div class="row">
    <div class="col-xl-12">
      <h3><strong>ショッピングカート</strong></h3>
      <div class="text-right">
        <%= link_to "カートを空にする", cart_items_path, method: :delete, class: "btn btn-danger mb-3" %>
      </div>


    <table class="table table-info table-bordered">
      <tr>
        <td class="col-md-4">商品名</td>
        <td class="col-md-2">単価（税込）</td>
        <td class="col-md-2">数量</td>
        <td class="col-md-2">小計</td>
        <td class="col-md-2"></td>
      </tr>
      <% @cart_items.each do |cart_item| %> <%# 文字列が〜〜と出てしまったら、＝が入ってないか確認。 %>
      <tr>
        <td class="col-md-4"><%= cart_item.item.name %></td> <%# 複数形だと、全てのデータとなってしまうので、一つのデータを取ってくるなら、単数形で記述 %>
        <td class="col-md-2"><%= cart_item.item.with_tax_price %></td> <%# cart_itemの中のアイテムの消費税（modelに追加した計算式で税込に） %>
        <td class="col-md-2">
        </td>
        <td class="col-md-2"><%= cart_item.subtotal %></td>
        <td class="col-md-2">
          <%= link_to "削除する", cart_item_path(cart_item), method: :delete, class: "btn btn-danger" %> <%# Rails routesで確認した時に、"URL ~~:id" がある場合は（引数）が必要 %>
        </td>
      </tr>
      <% end %>
    </table>
    <%= link_to "買い物を続ける", items_path, class: "btn btn-primary" %>
    
    <%= link_to "情報入力に進む", new_order_path, class: "btn btn-success" %>
  </div>
</div>